let x = 100 // think of them as values not variables, immutable

let strVal = "hi there"

let boolVal = true

let floatVal = 12.3

let decimalVal = 47.99m

let square(x) = x * x
let square2 x = x * x

let add x y =
    x + y

